# esbuild-compressor

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build esbuild-compressor` to build the library.

## Running unit tests

Run `nx test esbuild-compressor` to execute the unit tests via [Jest](https://jestjs.io).
